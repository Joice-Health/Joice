{
  "$schema": "https://turborepo.dev/schema.json",
  "ui": "tui",
  "globalDependencies": [".env"],
  "globalEnv": [
    "DATABASE_URL",
    "PORT",
    "NODE_ENV",
    "WEB_ORIGIN",
    "NEXT_PUBLIC_API_URL",
    "NEXT_PUBLIC_APP_URL",
    "NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY",
    "CLERK_PUBLISHABLE_KEY",
    "CLERK_SECRET_KEY",
    "KLAVIYO_API_KEY",
    "KLAVIYO_LIST_ID",
    "API_URL_INTERNAL",
    "RAG_MODEL",
    "BEDROCK_REGION",
    "POLLY_VOICE_ID",
    "NOTES_BUCKET",
    "TRUSTED_PROXY_HOPS",
    "NOTES_DIR"
  ],
  "tasks": {
    // Markdown is documentation, not input to any task, so a README or CLAUDE.md
    // edit does not bust the cache. Note that `turbo ls --affected` (used by
    // deploy.yml to pick which apps to ship) is package-level and ignores these
    // input globs.
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "!**/*.md"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "!**/*.md"]
    },
    "type-check": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "!**/*.md"]
    },
    "test": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "!**/*.md"]
    },
    "db:generate": {
      "cache": false
    },
    "db:migrate": {
      "cache": false
    }
  }
}
