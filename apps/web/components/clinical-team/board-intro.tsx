import { PageIntro } from '@/components/ui/page-intro';

export function BoardIntro() {
  return (
    <PageIntro eyebrow="Clinical team" title="The board behind every protocol.">
      Licensed clinicians who set, review, and stand behind everything Joice offers. Nothing
      ships without their sign-off: not a protocol, not a source, not a claim.
    </PageIntro>
  );
}
